comment: false

coverage:
  status:
    project:
      default:
        target: auto
    patch:
      default:
        target: auto

notify:
  after_n_builds: 1
  wait_for_ci: false

github_checks:
  annotations: false

slack:
  default: false

email:
  default: false
